---
deployment:
  branches:
    - main
  tasks:
    # Set paths
    - export REPODIR=/home3/iunfbcod/repositories/xazarwood.ir
    - export DEPLOYPATH=/home3/iunfbcod

    # Copy all files from repo to project root without deleting existing files
    - /bin/cp -R $REPODIR/* $DEPLOYPATH

    # Fix permissions for copied files
    #- /bin/chmod -R 755 $DEPLOYPATH
    #- /bin/chown -R hrbldaru:hrbldaru $DEPLOYPATH
